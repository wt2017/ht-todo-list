（1）增补操作系统的 C&P 压力测试，特别是 IO（包括 memory、disk、nic）
	（a）fio -> 块设备
	（b）iperf3 -> 网络 IO
	（c）stress-ng -> 内存

（1.1）得出软件系统资源的规划
（1.2）提供各种性能工具

（2）开发私有内核模块，捕获 Linux Panic 触发时的所有 OS 关键信息
	（a）kernel.panic = 10                    # panic 等待关键信息抓取结束后重启
	（b）具体信息包括：
                      # 1. 系统基本信息
                      # 2. 最后的（1000条？）内核日志 dmesg
                      # 3. 内核环形缓冲区
                      # 4. 硬件错误检查，arm/intel 总线错误寄存器（如 AER）读取
                      # 5. 内存状态
                      # 6. CPU状态
                      # 7. 内核模块状态
                      # 8. 进程状态快照
                      # （随着出现的 exceptional case 增补）
     (c) 支持各种 case
         （1）fs 不可用，包括 disk 不可用
         （2）网络不可用

（3）开发 gitlab CICD
	（a）应用层软件 case 直接纳入 CICD 框架
		（i）有相对完善的针对规约的测试集
		（ii）对软件性能类测试集比较稀缺，特别是 数据库 性能等方面的 测试集，其直接影响到了 系统/OS 的 IO 性能
	（b）嵌入式 - 业务/主卡目前当前除了 bsp/uboot 并无私有驱动迭代开发，可以考虑将系统测试 case 做进 CICD，周期性运行即可，无需代码触发
		（i）挑选一两款经典产品，比如 818 ？
		（ii）考虑将来驱动迭代开发的可能性
	（c）嵌入式 - 采集卡
		（i）和业务/主卡之间通信不畅通 （低速 IIC），是否自动化升级 bin 需要慎重考虑

（4）版本的管理维护
	（a）是否和 gitlab 集成？目前状态比较困难
	（b）推进标准化版本发布？

（5）部署 Taiga 做需求管理
	（a）提炼所有产品的通用需求
	（b）分解各个产品的特殊需求，规格差异
	（c）和 gitlab CICD 互联，得以看到所有需求的测试覆盖

